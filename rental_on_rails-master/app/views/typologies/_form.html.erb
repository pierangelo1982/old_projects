<%= form_for(@typology) do |f| %>
  <% if @typology.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@typology.errors.count, "error") %> prohibited this typology from being saved:</h2>

      <ul>
      <% @typology.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
