<p id="notice"><%= notice %></p>

<div class="container">

  <h3 class="page-title">Utenze – censimento utenze - S.7<br>
  AZIENDE: <%= @utility.company.denominazione %>
</h3>

<h5>ELENCO MACCHINARI</h5>
<table class="table">
  <thead class="thead-dark">
    <tr>
      <th>Nome</th>
      <th>Marca</th>
      <th>Matricola</th>
      <th>Potenza</th>
      <th>Ore</th>
      <th>Vettore</th>
      <th>Funzione</th>
      <th>Ausiliari</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @utility.utility_items.each do |utility_item| %>
      <tr>
        <td><%= utility_item.nome %></td>
        <td><%= utility_item.marca %></td>
        <td><%= utility_item.matricola %></td>
        <td><%= utility_item.potenza %></td>
        <td><%= utility_item.ore %></td>
        <td><%= utility_item.vettore %></td>
        <td><%= utility_item.funzione %></td>
        <td><%= utility_item.ausiliari %></td>
        <td><%= link_to 'Show', utility_item %></td>
        <td><%= link_to 'Edit', edit_utility_item_path(utility_item) %></td>
        <td><%= link_to 'Destroy', utility_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<p>
  <strong>Note:</strong>
  <%= @utility.note %>
</p>

<%= link_to 'Edit', edit_utility_path(@utility) %> |
<%= link_to 'Back', utilities_path %>

</div>
