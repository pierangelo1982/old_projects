<%= form_with(model: compressor_item, local: true) do |form| %>
  <% if compressor_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(compressor_item.errors.count, "error") %> prohibited this compressor_item from being saved:</h2>

      <ul>
      <% compressor_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :compressor_id %>
    <%= form.text_field :compressor_id, id: :compressor_item_compressor_id %>
  </div>

  <div class="field">
    <%= form.label :marca %>
    <%= form.text_field :marca, id: :compressor_item_marca %>
  </div>

  <div class="field">
    <%= form.label :modello %>
    <%= form.text_field :modello, id: :compressor_item_modello %>
  </div>

  <div class="field">
    <%= form.label :potenza %>
    <%= form.text_field :potenza, id: :compressor_item_potenza %>
  </div>

  <div class="field">
    <%= form.label :anno %>
    <%= form.text_field :anno, id: :compressor_item_anno %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_area :note, id: :compressor_item_note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
